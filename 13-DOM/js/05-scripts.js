//querySelectorAll

const card = document.querySelectorAll('.card');
console.log(card);

const formularios = document.querySelectorAll('#formulario');
console.log(formularios);

//Si un elemnto no existe 
const noExite = document.querySelectorAll('no-existe');
console.log(noExiste);
//NO Va regresar un no-list es como si estuviera vacia 